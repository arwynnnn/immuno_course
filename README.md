## venv
### linux
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install pandas requests tqdm
```

## windows
```bash
python3 -m venv .venv
source .venv\Scripts\activate
pip install pandas requests tqdm
```